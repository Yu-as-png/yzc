# 微头条 · 网页 B/S 版 — 功能原子化拆解

> 所有代码位于 `web/src/` 下；行号以实际文件为准。`api.js` 对 `code !== 0` 抛异常，页面统一 `try/catch` 处理。

## 1. 登录

- **组件/函数**：`Login.vue` → `submit()`，`web/src/views/Login.vue:18-48`
- **接口**：`POST /api/auth/login`；成功后写 localStorage 并按 `redirect` 回跳

```js
const data = await api.post('/auth/login', { username: f.username, password: f.password });
localStorage.setItem('token', data.token);
localStorage.setItem('user', JSON.stringify(data.user));
router.push(String(route.query.redirect || '/'));
```

前端预校验：用户名/密码非空（`Login.vue:21-24`）。

## 2. 注册（昵称 + 确认密码校验）

- **组件/函数**：`Login.vue` → `submit()` 注册分支，`web/src/views/Login.vue:25-38`

```js
if (mode.value === 'register') {
  if (!f.nickname) { err.value = '请输入昵称'; return; }
  if (f.password !== f.confirm) { err.value = '两次密码不一致'; return; }
}
...
await api.post('/auth/register', {
  username: f.username, password: f.password, confirm: f.confirm, nickname: f.nickname,
});
mode.value = 'login';
```

## 3. 路由登录守卫（未登录拦截 / 已登录跳首页）

- **组件/函数**：`web/src/router.js:19-25`

```js
router.beforeEach((to) => {
  const token = localStorage.getItem('token');
  if (!to.meta.public && !token) return { name: 'login', query: { redirect: to.fullPath } };
  if (to.name === 'login' && token) return { name: 'home' };
});
```

## 4. 退出登录

- **组件/函数**：`App.vue` → `logout()`，`web/src/App.vue:33-37`

```js
function logout() {
  localStorage.removeItem('token');
  localStorage.removeItem('user');
  router.push('/login');
}
```

## 5. 导航栏关键字搜索

- **组件/函数**：`App.vue` → `doSearch()`，`web/src/App.vue:27-31`

```js
function doSearch() {
  if (!q.value.trim()) return;
  router.push({ path: '/', query: { q: q.value.trim() } });
  q.value = '';
}
```

Home 挂载时读取 `route.query.q` 作为初始关键字（`Home.vue:58`）。

## 6. 首页信息流查询（关键字/类型/排序/分页）

- **组件/函数**：`Home.vue` → `load()`，`web/src/views/Home.vue:27-41`
- **接口**：`GET /api/headlines?keyword&typeId&sort&page&pageSize`

```js
const data = await api.get('/headlines?' + params.toString());
list.value = data.list;
total.value = data.total;
```

频道切换 `pickChannel`（`Home.vue:48-52`）、翻页 `prev/next`（`Home.vue:54-55`）、排序下拉（`Home.vue:96-101`，time/like/comment/view）。

## 7. 首页侧栏并行加载（公告/类型/热榜）

- **组件/函数**：`Home.vue` → `onMounted`，`web/src/views/Home.vue:57-72`

```js
const [n, t, h] = await Promise.all([
  api.get('/notices'),
  api.get('/types'),
  api.get('/headlines?sort=like&page=1&pageSize=10'),
]);
notices.value = n;
types.value = t;
hot.value = h.list;
```

## 8. 公告搜索（侧栏展开）

- **组件/函数**：`Home.vue` → `searchNotices()`，`web/src/views/Home.vue:77-84`
- **接口**：`GET /api/notices?keyword=...`

```js
async function searchNotices() {
  try {
    noticeList.value = await api.get('/notices', { keyword: noticeKeyword.value });
    showNotices.value = true;
  } catch (e) { err.value = e.message; }
}
```

## 9. 查看详情（浏览量自增）

- **组件/函数**：`Detail.vue` → `loadDetail()`，`web/src/views/Detail.vue:30-32`
- **接口**：`GET /api/headlines/:id`（服务端 view_count+1）

```js
async function loadDetail() {
  headline.value = await api.get('/headlines/' + id);
}
```

## 10. 评论列表（楼中楼两级结构）

- **组件/函数**：`Detail.vue` → `loadComments()` + `topComments/childrenOf`，`web/src/views/Detail.vue:19-20`、`34-36`

```js
const topComments = computed(() => comments.value.filter((c) => c.parent_cid === 0));
function childrenOf(cid) { return comments.value.filter((c) => c.parent_cid === cid); }
```

## 11. 点赞 / 取消点赞（toggle）

- **组件/函数**：`Detail.vue` → `toggleLike()`，`web/src/views/Detail.vue:38-47`
- **接口**：`POST /api/headlines/:id/like`

```js
const data = await api.post(`/headlines/${id}/like`);
headline.value.liked = data.liked;
headline.value.like_count = data.likeCount;
```

未登录用户只见「登录后可点赞评论」提示（`Detail.vue:103`）。

## 12. 发表评论 / 回复评论

- **组件/函数**：`Detail.vue` → `submitComment()`，`web/src/views/Detail.vue:49-65`
- **接口**：`POST /api/headlines/:id/comments`，body `{ content, parentCid }`

```js
if (!content.value.trim()) { err.value = '评论内容不能为空'; return; }
if (content.value.length > 500) { err.value = '评论不能超过 500 字'; return; }
await api.post(`/headlines/${id}/comments`, {
  content: content.value,
  parentCid: replyTo.value ? replyTo.value.id : 0,
});
content.value = '';
replyTo.value = null;
await loadComments();
```

回复目标通过 `replyTo = { id, author }` 设置（`Detail.vue:132`）。

## 13. 删除评论（本人或管理员）

- **组件/函数**：`Detail.vue` → `removeComment()`，`web/src/views/Detail.vue:67-76`；权限渲染 `Detail.vue:133`、`144`
- **接口**：`DELETE /api/comments/:cid`

```js
async function removeComment(cid) {
  if (!confirm('确认删除该评论？（其下回复也会被删除）')) return;
  await api.del('/comments/' + cid);
  await loadComments();
  headline.value.comment_count = comments.value.length;
}
```

```html
<a v-if="me && (me.id === c.uid || me.role === 1)" @click="removeComment(c.id)">删除</a>
```

## 14. 发布头条（前端校验）

- **组件/函数**：`Publish.vue` → `submit()`，`web/src/views/Publish.vue:17-33`
- **接口**：`POST /api/headlines`

```js
if (!f.title.trim()) { err.value = '标题不能为空'; return; }
if (!f.content.trim()) { err.value = '内容不能为空'; return; }
if (f.content.length > 5000) { err.value = '内容不能超过 5000 字'; return; }
if (!f.typeId) { err.value = '请选择类型'; return; }
const data = await api.post('/headlines', { title: f.title, content: f.content, typeId: Number(f.typeId) });
router.push('/headlines/' + data.id);
```

类型下拉与字数统计（`Publish.vue:46-53`）。

## 15. 我的头条查询（uid 过滤 + 分页）

- **组件/函数**：`Mine.vue` → `load()`，`web/src/views/Mine.vue:20-35`
- **接口**：`GET /api/headlines?uid=...&keyword&typeId&page&pageSize`

```js
const params = new URLSearchParams();
params.set('uid', String(me.value.id));
if (keyword.value) params.set('keyword', keyword.value);
if (typeId.value) params.set('typeId', String(typeId.value));
const data = await api.get('/headlines?' + params.toString());
```

## 16. 行内修改头条（类型不可改）

- **组件/函数**：`Mine.vue` → `startEdit()/saveEdit()`，`web/src/views/Mine.vue:52-68`
- **接口**：`PUT /api/headlines/:id`（仅 title/content）

```js
function startEdit(h) {
  editing.value = { id: h.id, title: h.title, content: h.content, typeName: h.typeName };
}
...
await api.put('/headlines/' + e.id, { title: e.title, content: e.content });
editing.value = null;
okMsg.value = '修改成功';
load();
```

## 17. 删除头条（二次确认 + 级联）

- **组件/函数**：`Mine.vue` → `removeOne()`，`web/src/views/Mine.vue:41-50`
- **接口**：`DELETE /api/headlines/:id`

```js
async function removeOne(h) {
  if (!confirm(`确认删除头条「${h.title}」？相关点赞和评论将一并删除！`)) return;
  await api.del('/headlines/' + h.id);
  okMsg.value = '删除成功';
  load();
}
```

## 18. 个人资料查看

- **组件/函数**：`Profile.vue` → `load()`，`web/src/views/Profile.vue:12-14`
- **接口**：`GET /api/auth/profile`

```js
async function load() {
  profile.value = await api.get('/auth/profile');
}
```

## 19. 修改密码（成功后强制重新登录）

- **组件/函数**：`Profile.vue` → `changePassword()`，`web/src/views/Profile.vue:16-34`
- **接口**：`PUT /api/auth/password`

```js
if (f.newPassword !== f.confirm) { err.value = '两次新密码不一致'; return; }
await api.put('/auth/password', f);
okMsg.value = '密码修改成功，请重新登录';
setTimeout(() => {
  localStorage.removeItem('token');
  localStorage.removeItem('user');
  router.push('/login');
}, 1500);
```

## 20. 管理后台 Tab 分发

- **组件/函数**：`Admin.vue` → `switchTab()`，`web/src/views/Admin.vue:161-172`

```js
function switchTab(t) {
  tab.value = t;
  err.value = '';
  ({
    users: loadUsers, headlines: loadHeadlines, types: loadTypes,
    notices: loadNotices, hot: loadHot, comments: loadComments,
  }[t]());
}
```

非管理员挂载即返回（`Admin.vue:174-177`）。

## 21. 用户列表 + 活跃度统计

- **组件/函数**：`Admin.vue` → `loadUsers()`，`web/src/views/Admin.vue:44`
- **接口**：`GET /api/users`、`GET /api/stats/users`

```js
async function loadUsers() {
  users.value = await api.get('/users');
  statUsers.value = await api.get('/stats/users');
}
```

表格展示发布数/评论数/获赞数/活跃度（`Admin.vue:196-213`）。

## 22. 头条全量管理（查询/修改/删除）

- **组件/函数**：`Admin.vue` → `loadHeadlines()/startEditHeadline()/saveHeadline()/removeHeadline()`，`web/src/views/Admin.vue:46-51`、`147-159`、`138-145`
- **接口**：`GET /api/admin/headlines?keyword&uid`、`PUT /api/headlines/:id`、`DELETE /api/headlines/:id`

```js
async function loadHeadlines() {
  const params = new URLSearchParams();
  if (hKeyword.value) params.set('keyword', hKeyword.value);
  if (hUid.value) params.set('uid', hUid.value);
  headlines.value = await api.get('/admin/headlines?' + params.toString());
}
```

管理员修改时可改类型（`Admin.vue:226-230`），普通用户编辑态类型锁定（Mine.vue）。

## 23. 类型管理（新增/改名/删除）

- **组件/函数**：`Admin.vue` → `addType()/saveType()/removeType()`，`web/src/views/Admin.vue:55-81`
- **接口**：`POST /api/types`、`PUT /api/types/:id`、`DELETE /api/types/:id`

```js
async function addType() {
  if (!newType.value.trim()) return;
  await api.post('/types', { name: newType.value.trim() });
  newType.value = '';
  flash('类型添加成功');
  loadTypes();
}
```

## 24. 公告管理（发布/修改/删除）

- **组件/函数**：`Admin.vue` → `addNotice()/saveNotice()/removeNotice()`，`web/src/views/Admin.vue:85-112`
- **接口**：`POST /api/notices`、`PUT /api/notices/:id`、`DELETE /api/notices/:id`

```js
async function addNotice() {
  const f = noticeForm.value;
  if (!f.title.trim() || !f.content.trim()) { fail(new Error('标题和内容不能为空')); return; }
  await api.post('/notices', { ...f });
  noticeForm.value = { title: '', content: '', remark: '' };
  flash('公告发布成功');
  loadNotices();
}
```

## 25. 热门排行（多维度排序 + 分页）

- **组件/函数**：`Admin.vue` → `loadHot()`，`web/src/views/Admin.vue:114-119`；排序切换 `Admin.vue:304-308`
- **接口**：`GET /api/stats/hot?sort=like|comment|view&page&pageSize`

```js
async function loadHot() {
  const params = new URLSearchParams({ sort: hotSort.value, page: String(hotPage.value), pageSize: String(hotPageSize) });
  const data = await api.get('/stats/hot?' + params.toString());
  hotList.value = data.list;
  hotTotal.value = data.total;
}
```

## 26. 评论管理（多维筛选 + 删除）

- **组件/函数**：`Admin.vue` → `loadComments()/removeComment()`，`web/src/views/Admin.vue:121-136`
- **接口**：`GET /api/admin/comments?hid&uid&keyword`、`DELETE /api/comments/:id`

```js
async function loadComments() {
  const params = new URLSearchParams();
  if (cHid.value) params.set('hid', cHid.value);
  if (cUid.value) params.set('uid', cUid.value);
  if (cKeyword.value) params.set('keyword', cKeyword.value);
  comments.value = await api.get('/admin/comments?' + params.toString());
}
```

## 27. 统一请求封装（异常驱动）

- **组件/函数**：`web/src/api.js:2-29`

```js
export const api = {
  get: (path, params) => request(path + (params ? '?' + new URLSearchParams(params) : '')),
  post: (path, body = {}) => request(path, { method: 'POST', body }),
  put: (path, body = {}) => request(path, { method: 'PUT', body }),
  del: (path) => request(path, { method: 'DELETE' }),
};
```

`code !== 0` 时 `throw new Error(json.msg)`（`api.js:20-22`），页面 `try/catch` 后写入 `err.value`。
